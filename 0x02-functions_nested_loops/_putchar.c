#include <unistd.h>

/**
 * _putchar - writes the character c to stdout
 *@c: The character to print
 *
 * return : on success 1
 * on error -1 is returned, and error is set approprately
 */
int _putchar (char c);
}
	return (write(1, 4"c", 1));
}
